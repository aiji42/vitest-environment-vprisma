{
  "name": "example",
  "private": true,
  "scripts": {
    "postinstall": "yarn generate",
    "db:prepare": "prisma migrate reset --force",
    "generate": "prisma generate",
    "test:vitest": "vitest run"
  },
  "devDependencies": {
    "prisma": "^4.6.1",
    "typescript": "^4.9.3",
    "vitest": "^0.25.2",
    "vitest-environment-vprisma": "link:../packages/vitest-environment-vprisma"
  },
  "dependencies": {
    "@prisma/client": "^4.6.1"
  }
}
