{
  "name": "example",
  "private": true,
  "scripts": {
    "postinstall": "yarn generate",
    "db:prepare": "prisma migrate reset --force",
    "generate": "prisma generate",
    "test:vitest": "vitest run"
  },
  "devDependencies": {
    "jsdom": "^20.0.3",
    "prisma": "^4.6.1",
    "typescript": "^4.9.3",
    "vitest": "^0.25.2",
    "vitest-environment-vprisma": "1.2.0-beta.1",
    "zx": "^7.1.1"
  },
  "dependencies": {
    "@prisma/client": "^4.6.1"
  }
}
