{
  "name": "example",
  "private": true,
  "scripts": {
    "postinstall": "yarn generate",
    "db:prepare": "prisma migrate reset --force",
    "generate": "prisma generate",
    "test:vitest": "vitest run"
  },
  "devDependencies": {
    "jsdom": "^23.0.1",
    "prisma": "^5.7.0",
    "typescript": "^5.3.3",
    "vitest": "^1.0.2",
    "vitest-environment-vprisma": "1.3.0-beta.1",
    "zx": "^7.2.3"
  },
  "dependencies": {
    "@prisma/client": "^5.7.0"
  }
}
